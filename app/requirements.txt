# 애플리케이션(trailwalk) 의존성. 서빙 쪽과 별개다.
#
#   pip install -r app/requirements.txt
#   playwright install chromium      # kakao provider 를 쓸 때만

pillow>=11            # 이미지 디코드/리사이즈/JPEG 인코드 — imaging.py 필수

# 설정 파일(app/config/trailwalk.yaml) 파싱 — settings.py 필수.
# .env 로더(config.py)가 python-dotenv 를 안 쓰는 것과 대조적으로 여기는
# 남의 라이브러리를 쓴다: YAML 은 "KEY=VALUE 한 줄" 과 달리 직접 짤 만한
# 형식이 아니고, 잘못 짠 파서는 설정을 조용히 틀리게 읽는다.
pyyaml>=6

# kakao provider 전용. Kakao 로드뷰에 서버사이드 REST 가 없어서 브라우저를 몬다.
# → app/docs/21-roadview-providers.md
playwright>=1.48

# equirect_to_view() 전용. 렌더된 뷰포트만 쓰는 provider 에서는 필요 없다.
numpy>=2.0

# ── 개발용 (하네스) ────────────────────────────────────────────────────────
#
#   uv pip install --python .venv/bin/python pytest ruff
#   .venv/bin/python -m pytest    # 206개 · 1초 미만. 브라우저도 서버도 필요 없다
#   .venv/bin/ruff check .
#
# 설정은 루트 pyproject.toml 에 있다. → docs/12-harness.md
pytest>=8
ruff>=0.7

# 웹 UI(app/run_web.py) 전용. 표준 라이브러리 http.server 로도 라우트 몇 개는
# 되지만, 뷰포트 쿼리(bbox float 4개 + 필터)의 검증을 직접 짜면 잘못된 입력이
# 400 이 아니라 500 으로 새기 쉽다 — 이 레포가 제일 경계하는 조용한 실패라
# 검증을 프레임워크에 맡긴다 (틀린 필드를 422 로 짚어 준다). yaml 파서를
# 직접 안 짜는 것과 같은 결이다.
fastapi>=0.115
uvicorn>=0.30

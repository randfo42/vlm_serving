# 레이블 — 무엇이 산책로인가

출처: 서울시 「서울, 테마산책길」 https://gil.seoul.go.kr/trail/list.do?key=2406040004
수집: 2026-08-17 · `labels/fetch_gil_seoul.py` → `labels/trails.json`

---

## 1. 얻은 것

**150개 산책로, 총 421.3km.** 사이트가 밝힌 "5개 테마 총 150개소" 와 개수·테마별
분포가 정확히 일치한다 (2015~2018년 선정).

| 테마 | 개수 |
|---|---:|
| 숲이 좋은 길 | 75 |
| 역사문화길 | 24 |
| 한강·하천이 좋은 길 | 23 |
| 전망이 좋은 길 | 23 |
| 계곡이 좋은 길 | 5 |

28개 지역에 걸쳐 있다 (종로 12, 노원 11, 마포·강서·강북 각 8 …).

항목 하나:

```json
{
  "trail_sn": 184,
  "name": "홍릉 두물길",
  "theme": "한강·하천이 좋은 길",
  "difficulty": "중급",
  "distance_km": 6.2,
  "duration": "2시간 10분",
  "gu": "동대문구",
  "detail_url": "https://gil.seoul.go.kr/trail/view.do?key=2405200010&sc_trailSn=184&sc_trailSeCd=SE004",
  "smgis_url": "https://map.seoul.go.kr/smgis/short2.jsp?p=6Pn4G"
}
```

`--detail` 을 주면 상세 페이지까지 훑어 **경유지 이름**(신설동역 → 안암2교 →
성북천교 → 청계천 → …)과 진입 방법, 코스 지도 PDF 링크가 붙는다.

목록 페이지 하나에 이름·테마·난이도·거리·시간·자치구·지도링크가 전부 들어 있어서,
기본 대장을 만드는 데는 상세 페이지 150건을 훑을 필요가 없다 (15요청, ~15초).

---

## 2. ⚠️ 없는 것 — 좌표

**대장에는 경로 폴리라인이 없다.** 한동안 이게 평가의 가장 큰 구멍이었다 —
§7 의 제4의 길(경유지 지오코딩 + 도보 길찾기)로 뚫었다. 아래는 그 이전에
확인한 것들이고, 어디에 좌표가 **없는지**의 기록으로 남긴다.

확인한 것들:

| 후보 | 결과 |
|---|---|
| 상세 페이지의 "코스자료 다운로드" | **GPX 가 아니라 코스 지도 PDF** (2쪽) |
| 공공데이터포털 「전국길관광정보표준데이터」 | 길명·총길이·시종점 **이름**·경로정보(텍스트)뿐. **위경도 없음** |
| 「서울특별시 양천구_둘레길 현황」 등 자치구 데이터 | 일부 구만. 전 시 커버 안 됨 |
| 스마트서울맵 `map.seoul.go.kr` | **코스 선을 실제로 그린다.** 여기 있다 |

레이블 항목마다 `smgis_url` 을 남겨둔 이유가 이것이다. 지금은 사람이 눌러 보는
용도이지만, 좌표를 꺼낼 때 유일한 손잡이가 된다.

---

## 3. 좌표를 얻는 세 가지 길

~~아직 고르지 않았다.~~ **셋 다 안 골랐다** — §7 의 네 번째 길로 갔다.
아래는 각 후보의 장단 기록이다. (a) 스마트서울맵은 형태 2(경로 커버리지)를
재려면 여전히 필요하다.

### (a) 스마트서울맵에서 꺼낸다

단축 링크가 `poiViewMap?ti=100715&pi=<poi_id>` 로 리다이렉트한다.
`ti=100715` 이 테마산책길 테마 id 이고, 150건이 같은 테마에 들어 있다.
일부 항목은 `pi` 가 타임스탬프가 아니라 **안정적인 id**(`A_015`, `C_039`)로 나온다.

페이지 JS 에서 `smgis2ContextJs + '/qry/THEME'` 라는 질의 엔드포인트를 찾았다.
GET 은 400, POST 는 500 을 준다 — **존재하지만 페이로드 형식을 아직 모른다.**
브라우저 개발자도구에서 실제 요청을 한 번 보면 풀린다.

- 장점: 정확히 이 150개 코스의 공식 선형이다. 레이블과 1:1
- 단점: 비공식 스크래핑. 언제든 깨진다

공공데이터포털에 「서울특별시_스마트서울맵」 OpenAPI 가 등록돼 있다. 이게 같은
데이터를 공식 창구로 준다면 그쪽이 낫다. **미확인.**

### (b) OpenStreetMap 으로 대체한다

Overpass API 로 서울 영역의 `highway=footway|path|pedestrian|steps` 를 받는다.

- 장점: 무료·개방·즉시 사용 가능. 서울 보행로 커버리지가 좋다
- 단점: **서울시가 지정한 150개 코스와 다른 대상이다.** OSM 의 보행로에는
  테마산책길이 아닌 것이 대량으로 섞이고, 반대로 코스 일부가 빠질 수 있다.
  "산책로 판정" 의 정의가 미묘하게 달라진다

### (c) 손으로 시드만 찍는다

각 코스의 **출발점 좌표 하나 + 초기 방위**만 수동으로 정한다. 경유지 이름
(예: "신설동역 1번 출구")이 이미 있으니 지오코딩으로 상당 부분 자동화된다.

- 장점: 오늘 당장 된다. 탐색 루프의 입력으로는 **이것으로 충분하다**
- 단점: 스텝별 정답이 없다. "얼마나 잘 식별하는가" 를 재려면 결국 (a)나 (b)가 필요

---

## 4. 평가를 어떻게 할 것인가

정답의 형태가 두 가지로 갈린다. 섞지 말 것.

**형태 1 — 점 단위 정확도.** `(lat, lng, heading)` 마다 정답 라벨을 붙이고
`is_trail` 을 맞추는지 본다. 정밀도·재현율·ROC 가 나온다.
필요한 것: 코스 폴리라인(양성) + 일반 차도 표본(음성). → §3 (a) 또는 (b)

**형태 2 — 경로 단위 커버리지.** 코스 시작점에서 탐색을 돌려 실제 코스를 얼마나
따라갔는지 본다. 폴리라인과의 거리·중첩 길이로 잰다.
이쪽이 실제 목표("로드뷰를 진행하며 산책로를 이어간다")에 더 가깝지만,
채점하려면 역시 폴리라인이 있어야 한다.

**§3 (c) 만으로 할 수 있었던 것**: 탐색을 돌려 경로를 뽑고 지도에 올려 사람이
눈으로 보는 것. 정량 지표는 안 나오지만 **"완전히 헛짚는가" 는 바로 보인다.**
첫 실측은 이것으로 했다 (`23-open-questions.md` §1).

> **2026-08-19 현재 위치**: 형태 1 이 성립했다(§7). 다만 지금 세트는
> **양성 전용**이라 재현율만 나온다 — accuracy = recall 이고 precision·FPR·ROC
> 는 정의되지 않는다. 전역 오탐률은 §5 (b) 의 별도 음성 수집이 와야 한다.
> 리포트가 이 상태를 명시적으로 말한다 (`eval/report_eval.py`).

---

## 5. ~~음성 표본이 필요하다~~ — 폐기 (2026-08-19)

> **첫 문장은 여전히 참이다**: 양성만으로는 아무것도 못 잰다. 전부
> `is_trail=true` 라고 답해도 만점이다. 폐기된 것은 **음성을 어디서 얻는가** 다.

이 절은 세 가지 음성 후보를 권했고, 그중 마지막을 "가장 값싸고 가장 어려운
음성" 이라며 특히 밀었다 — *같은 좌표에서 산책로 반대 방향을 본 화각.*
그 권고대로 **orth 168 · rev 85 · offroute 82 장**을 만들었고, 전부 폐기한다.

### 왜 틀렸나

**1. 판정 기준과 모순된다.** `system_v3` 는 "카메라가 서 있는 지면" 을 묻는다
(v4 까지는 같다. ⚠️ **v5·v6 은 다르다** — 노면이 아니라 주변 자연을
묻고, 카메라가 그 위에 서 있는지를 아예 안 본다 → `23-open-questions.md` §5).
산책로 위 pano 에서 ±90° 를 봐도 카메라는 **여전히 산책로 위**다.
라벨의 단위는 `(pano, heading)` 이 아니라 **pano** 다.

**2. 정확히는 "틀린 라벨" 이 아니라 "질문이 다른 라벨" 이다.** v3 는 지면이
"a defined route runs under the camera **and continues ahead**" 라고 쓴다 —
*이어짐* 은 방위의 속성이다. ±90° 에서 모델이 내는 "아님" 은 *"이 지점이
산책로인가"* 의 답이 아니라 *"저쪽으로 길이 뻗어 있는가"* 의 답이고,
**그 답으로서는 맞다.** 두 질문의 답을 같은 라벨 칸에 넣은 것이 오류다.
(이 형태가 중요하다: "그럼 true 로 뒤집자" 는 반론까지 같이 막는다.)

> ⚠️ **이건 새 발견이 아니다. 레포가 하루 전에 이미 측정해뒀다.**
> `23-open-questions.md` §6(2026-08-18)이 같은 pano 를 여러 방위로 찍은
> 쌍의 일치율을 각도차별로 쟀다:
>
> | 각도차 | 쌍 | 일치율 |
> |---|---:|---:|
> | ~60° (축을 벗어남) | 53 | 26% |
> | ~120° | 23 | 65% |
> | **~180° (정반대 = 축 위)** | **19** | **100%** |
>
> 그 절의 마지막 문장이 정확히 이 결론이다 — *"교훈은 화각 얘기가 아니라
> **질문의 단위**다. 판정은 지점에 대한 것인데 캡처는 방향에 대한 것이라,
> 둘을 같은 표에 넣으면 안 된다."* 정반대 19쌍이 예외 없이 일치한다는 것은
> **`rev`(180°) 가 음성일 수 없다는 실측**이고, ±60° 의 26% 일치는
> `orth`(±90°) 가 다른 질문의 답이라는 실측이다.
> 그걸 읽고도 음성을 만든 것이 이 사고의 본체다.

**3. offroute 는 근거가 기하뿐이다.** "코스 폴리라인에서 200m 밖" 은
"산책로 아님" 을 뜻하지 않는다. 그 반례가 지금 우리 손에 있다 —
**서울 테마산책길 150개 421km** 대장(§1)이고, 종로구에만 12개가 더 있다.
격자로 뽑은 점이 실제로는 다른 코스 위였을 확률이 무시할 수준이 아니다.

### 그래서 화각은 화살표 방위만 쓴다

이건 절약이 아니다. **화살표(이웃 그래프의 `spot.pan`)는 "이 면이 저쪽으로
이어진다" 는 지도의 진술이고, v3 의 "continues ahead" 조건절을 관측 가능하게
만드는 유일한 값이다.** 애플리케이션(walk/explore)이 이웃 그래프의 화살표로만
질문하는 것과 정확히 같은 기준이며, 평가와 루프가 같은 질문을 하게 만든다.

코스 진행 방위는 **여러 화살표 중 어느 것을 고를지의 선택자로만** 쓴다.
진행 방위를 그대로 화각으로 쓰면 경유지가 길 건너 POI 인 지점에서 카메라가
옆 건물을 본다 (실측 사고 — 채택 화살표와의 차는 `arrow_diff_deg` 로 남긴다).

### 음성은 두 곳에서만 온다

**(a) 검수자가 `pos/` → `neg/` 로 옮긴 것.** 이 파이프라인에서 사람이 사진을
실제로 보고 만드는 유일한 라벨이다. 자동으로는 못 만드는 종류이기도 하다 —
정의상 "공식 코스 위 · 화살표 방위 · 로드뷰가 있는데 산책로가 아닌 장면"
이고, 그건 §7 에서 눈으로 확인한 청계천길 사례(라우터가 하천 산책로 대신
위쪽 차도를 탐)와 같은 집합이다.

**(b) "아예 산책로가 아닌 곳" 별도 수집 — TODO.** 이번 범위 밖이다.
`pos/` 에서 옮겨 만들지 않고 **새로 뽑는다.**

> ⚠️ **(a) 는 무작위 표본이 아니다.** "도보 라우터가 여기를 코스로 판단했다"
> 는 조건부 표본이라 하드 네거티브로는 이상적이지만 **서울 전역의 오탐률(FPR)
> 추정에는 쓸 수 없다.** 리포트에서 하나의 accuracy 로 합산하지 말고 독립
> 슬라이스로만 낼 것. 대장에는 `neg_origin: "review"` 로 출처를 남긴다.

---

## 6. 재수집

```bash
python app/labels/fetch_gil_seoul.py            # 목록만 (~15초)
python app/labels/fetch_gil_seoul.py --detail   # 경유지까지 (~3분)
```

파서는 페이지 구조 변경에 대비해 필드 누락 건수를 마지막에 찍는다.
`⚠ ... 누락 N건` 이 뜨면 사이트가 바뀐 것이니 파서를 먼저 고칠 것.

---

## 7. 2026-08-19 — 종로구 파일럿으로 §3 이 뚫렸다 (제4의 길)

§3 의 세 갈래 대신 사용자가 정한 네 번째 길로 진행했다:
**자치구 페이지의 텍스트 경유지 → 지오코딩 → 카카오맵 웹 도보 길찾기**
(엔드포인트 관찰 기록은 `24-course-routes.md`).

```
labels/fetch_jongno.py        종로 걷기코스 9개, 경유지 59개    → jongno/courses.json
labels/geocode_waypoints.py   54 지오코딩 + 5 skip(비 POI)     → jongno/waypoints.json
labels/fetch_walk_routes.py   45/45 구간 폴리라인               → jongno/courses_geom.json
labels/make_samples.py        리샘플→pano 캡처, true/false 자동 라벨 → jongno/samples.jsonl
labels/apply_review.py        사람 검수(파일 이동) 반영          → jongno/labels.jsonl
run_eval.py                   VLM eval (재개 가능)              → runs/*-eval.jsonl
eval/report_eval.py           정확도·혼동행렬·소스별 슬라이스·ROC 점
```

### 한계 두 가지 (검수가 반드시 필요한 이유)

1. **suspect 코스 3개 제외** — 도보 라우터가 산길·성곽길을 못 타고 도로로
   우회한다. 라우팅 길이가 공식 거리의 1.6배를 넘으면(흥인지문~혜화문 2.0x,
   삼청공원 2.2x, 화정박물관~서울예고 3.1x) 그 폴리라인 위 점은 산책로가
   아니라 인도라서 기본 제외한다.
2. **비율이 정상인 코스도 폴리라인이 차도를 탈 수 있다** — 청계천길은 라우팅이
   하천 산책로가 아니라 위쪽 청계천로(차도·인도)를 타서, positive 로 캡처된
   이미지 상당수가 실은 차도 장면이다 (눈으로 확인). **자동 라벨은 검수 전
   가설이다** — pos/ 폴더의 차도 장면을 neg/ 로 옮기는 것까지가 라벨링이다.

§4 의 형태1(점 단위)이 이것으로 성립한다. 형태2(경로 커버리지)는 여전히
스마트서울맵 폴리라인(§3a)이 필요하다 — 150개 코스 전체로 확장할 때 다시 본다.


---

## 8. 양성 전용 설계 (2026-08-19)

§5 폐기의 귀결. 지금 파이프라인이 만드는 것은 **양성뿐**이다.

### 라벨 세 층

| 필드 | 누가 쓰나 | 뜻 |
|---|---|---|
| `label` | `make_samples` | **검수 전 가설.** 전부 true (코스 폴리라인 위 pano) |
| `final_label` | `apply_review` | 확정. 폴더가 정한다 — `pos/`=true · `neg/`=false · `discard/`=제외 |
| `review` | `apply_review` | `kept` / `flipped` / `discarded` |

`flipped / (kept+flipped)` 이 **라우팅+스냅이 만든 라벨 오염률**이고, 양성 전용
설계에서 이게 파이프라인 품질의 유일한 정량 지표다. 그래서 `label` 을
"전부 true 라 무의미하다"고 지우면 안 된다 — 이 지표가 그 차이로 정의된다.

### 코스마다 시작 1km 만 쓴다

150코스 421km 를 50m 간격으로 전부 찍으면 캡처만 7시간이고, 같은 코스의 인접
50m 점은 거의 같은 장면이다. 정보를 사는 축은 밀도가 아니라 **코스 수**다 —
테마 5종 × 자치구 28곳의 다양성은 서로 다른 정보이지만, 한 코스 안의 20번째
점은 19번째와 거의 같다. (`sampling.head_m`)

> ⚠️ **알려진 편향**: 코스 앞머리에는 진입로·주차장·역 출구·안내판이 몰려 있다.
> 코스 중반의 전형적인 산책로 장면이 **과소 대표**된다. 이건 설계상 감수한
> 편향이고, 리포트를 읽을 때 반드시 같이 읽어야 한다.
> **TODO: 1km 이후 구간 수집** — 검수 부담과 캡처 시간이 확보되면 `head_m` 을
> 올리거나 구간별로 나눠 받는다.

### 검수 규모

경유지 817개를 전건 육안확인하는 것은 불가능하다. 자동 게이트로 대체한다:

| 게이트 | 무엇을 잡나 |
|---|---|
| 자치구 대조 (`gu_found`) | 동명 POI 가 딴 동네에 잡힌 것. 종로에서 "구름다리"가 9km 밖에 정확 일치로 잡혔다 |
| 거리 상한 (`cap_m`) | 위와 같은 사고의 기하적 방어 |
| 체인 길이 비 (`chain_ratio`) | 경유지 직선합 / 공식 거리. 코스당 스칼라 하나 |
| 라우팅 비 (`ratio` 1.6x) | 도보 라우터가 산길을 못 타고 도로로 우회 → `suspect` |
| 커버리지 (`on_route_ratio`) | 로드뷰가 실제로 코스 위에 있는가 (아래 §9) |

사람이 보는 것은 `courses_report.tsv` **한 장**(코스당 한 줄)이고, 위험 순으로
정렬해 상위 ~30줄만 눈으로 본다. 817건이 30줄이 된다.

## 9. 로드뷰 커버리지 — 없을 수 있다

**로드뷰는 차량 촬영이다.** 서빙 쪽에서 미리 경고했던 그대로,
숲길(75개)·계곡길(5개)은 커버리지가 없을 가능성이 크다. 이건 데이터 수집의
사고가 아니라 **애플리케이션 적용 범위의 사실**이다 — 로드뷰로 걸을 수 없는
산책로는 이 시스템이 다룰 수 없다.

그래서 커버리지를 캡처 전에 재고(`probe_coverage.py`), 결과를 산출물로 남긴다
(`<데이터셋>/coverage.json`). 두 수를 **따로** 낸다 — 합치면 거짓말이 된다:

- `pano_hit` — `nearest()` 가 반경 안에서 pano 를 준 비율
- `on_route` — 그 pano 가 **폴리라인 근처**(`sampling.snap_radius_m` × 1.5)인
  비율 ← 진짜 커버리지

숲길 옆 차도의 pano 가 30m 안에 있으면 `pano_hit` 은 잡히지만 그건 산책로가
아니다. 캡처 단계의 `off_polyline` 판정을 미리 당겨 하는 것과 같다.

> ❌ **예상이 빗나갔다 (2026-08-19 실측).** "ratio 게이트와 커버리지 게이트가
> 같은 코스를 문다"고 적어뒀는데 — 도보 라우터가 산길을 못 타는 코스는 로드뷰도
> 없으리라는 추론이었다 — **suspect 29개 중 커버리지도 낮은 것은 2개(7%)뿐**
> 이었다. **비-suspect 60개 중에서는 8개(13%)** 로 오히려 더 높다 —
> 두 게이트는 사실상 독립이고, 방향조차 예상과 반대다.
> (`seoul/courses_geom.json` 의 `suspect` × `coverage.json` 의
> `on_route_ratio < 0.5`, 89코스 전수)
>
> 왜 틀렸나: 우리가 쓰는 것은 **코스의 시작 1km** 다(§8). 산책로의 첫 1km 는
> 대개 공원 입구·역·주차장에서 시작하므로 촬영 차량이 닿는다. 로드뷰가 없는
> 것은 코스의 *안쪽*이고, 우리는 거기를 안 본다. 즉 `head_m` 컷이 커버리지가
> 가장 좋은 구간을 골라주고 있다 — 앞머리 편향(§8)의 또 다른 얼굴이다.
> **1km 이후 구간을 수집할 때는 커버리지가 크게 떨어질 것으로 봐야 한다.**

### 퍼널 (2026-08-19 실측 · `python app/labels/funnel.py --dataset seoul`)

| 테마 | 대장 | 경유지있음 | 지오코딩 | 라우팅 | ratio통과 | 커버리지 | 최종코스 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 숲이 좋은 길 | 75 | 69 | 41 | 41 | 25 | 36 | **21** |
| 역사문화길 | 24 | 22 | 17 | 16 | 14 | 15 | **12** |
| 한강·하천이 좋은 길 | 23 | 21 | 14 | 14 | 13 | 14 | **13** |
| 전망이 좋은 길 | 23 | 23 | 12 | 12 | 5 | 10 | **4** |
| 계곡이 좋은 길 | 5 | 5 | 5 | 5 | 3 | 4 | **2** |
| **합계** | **150** | **140** | **89** | **88** | **60** | **79** | **52** |

최종 샘플 **914장** (숲 359 · 하천 248 · 역사문화 211 · 전망 67 · 계곡 29),
25개 자치구. 후보 1019점 중 로드뷰 없음 81 · 중복 pano 24 가 빠졌고
**캡처 실패는 0건**이다. `arrow_diff` p50 2.5° — 화살표와 코스 진행 방위가
대체로 일치한다(p90 62.7° 는 코스가 길을 가로지르는 지점).

> 계곡길은 29장뿐이다(대장 5코스 중 2코스 통과). 테마별 정확도를 볼 때
> 이 수로는 결론을 낼 수 없다 — 그렇게 읽어야 한다.

각 게이트가 무엇을 걸렀나:

- **150 → 140** 상세페이지에 경유지가 없는 코스
- **140 → 89** 지오코딩. 경유지 2개 미만 22개 + 좌표를 믿을 수 없는 39개.
  후자는 경유지가 `T1`~`T6`(문화비축기지 탱크 번호)이나 **"3번 출구로 나와
  잠실대교 방향으로"** 같은 *문장* 이라 엉뚱한 POI 에 붙은 것들이다.
  `chain_ratio`(경유지 직선합 ÷ 공식거리)가 잡는다 — 실제 걷는 길이 직선보다
  짧을 수 없으므로 1.5 초과는 좌표가 틀린 것이다 (최악: 뚝섬 숲속길 10.44)
- **88 → 60** 도보 라우터가 산책로 대신 도로로 우회 (ratio 1.6x 초과)
- **커버리지 79/89** 로드뷰가 코스 위에 있는 코스. `nearest` 실패 0건이고,
  `pano_hit − on_route` 평균이 0.00 이다 — pano 가 잡히면 그건 코스 위였고,
  "옆 차도에 붙는" 우려는 이 세트에서는 일어나지 않았다
- **최종 52코스** = ratio 통과 ∧ 커버리지 통과

전망길이 23 → 4 로 가장 심하게 깎인다(라우팅 우회 7건). 조망 지점은 능선·정상
이라 도보 라우터가 우회로를 만들기 때문이다. **이 세트는 하천·시가지 쪽으로
기울어 있다** — 결과를 읽을 때 같이 읽어야 한다.

### 데이터셋을 나누는 이유

`jongno`(자치구 걷기코스 9개, 전 구간)와 `seoul`(테마산책길 150개, 시작 1km)은
**합치지 않는다**:

- **지리적 중복** — 테마산책길에도 종로구 코스가 12개 있다. 합치면 같은 pano 를
  두 course_id 로 두 번 평가하게 된다 (dedupe 는 런 안에서만 작동한다)
- **샘플링 밀도가 다르다** — 합쳐서 하나의 accuracy 를 내면 소수 코스가 수치를 지배한다
- **역할이 다르다** — jongno = 소형·회귀 세트, seoul = 규모·다양성 세트

## 10. 수집 시점에 얻을 수 있는 라벨 전부 (2026-08-20)

지금 `samples.jsonl` 에 남기는 필드는 **얻을 수 있는 것의 일부**다. 아래는
"이미 부르고 있는 API 가 이미 주고 있는데 우리가 버리는 것" 까지 포함한 전수다.

수집이 끝난 뒤에 "이 표본은 왜 이런가" 를 물으면 대개 **버린 필드에 답이 있다.**
`shot_tool` 이 그랬다 — 914장의 80%가 차량 촬영이었다는 사실을 넉 달치 파이프라인이
끝나고 나서야 알았고, 그 필드는 처음부터 응답에 있었다. 그래서 **판단에 안 쓰더라도
기록은 한다.** 기록은 공짜고 재수집은 1.5시간이다.

### 10.1 출처별 — 무엇을 언제 얻는가

| # | 출처 | 언제 | 추가 비용 |
|---|---|---|---|
| A | 대장(자치구 페이지 / `gil.seoul.go.kr`) | 코스 확정 시 | 0 |
| B | 도보 길찾기 `walkset.json` | 라우팅 시 | 0 (이미 호출) |
| C | 로드뷰 반경 검색 `v2/nodes` | 스냅 시 | 0.1s/점 |
| D | 로드뷰 단건 `v2/node/{id}` | 선택 | 0.1s/점 |
| E | Kakao Local REST | 선택 | 0.05s/점 |
| F | 계산으로 얻는 것 | 항상 | 0 |

### 10.2 전수 목록

**A. 코스 대장** — 코스당 1회

| 라벨 | 값 | 지금 쓰나 |
|---|---|---|
| `theme` | 숲길 / 하천 / 전망 / 역사문화 / 계곡 | ✅ 퍼널 |
| `gu` | 자치구 | ✅ 지오코딩 바이어스 |
| `distance_km` | 공식 거리 | ✅ ratio 게이트 |
| `waypoints[]` | 경유지 이름 | ✅ |
| 경유지 개수 / 코스 길이 | | ❌ 코스 복잡도 |

**B. 도보 길찾기** (→ `24-course-routes.md` §3.1) — 이미 받고 있고 전부 버린다

| 라벨 | 값 | 지금 쓰나 |
|---|---|---|
| `link.existCenterLine` | `true` → 중앙선 = 차도 | ❌ **도보 3% vs 33%. 최우선** |
| `section.facilities` | `{stair, underground, bridge}` | ❌ 계단 = 차량 진입 불가 |
| `guideMent` | `계단이용` `징검다리로 횡단` `교량 진입` | ❌ 구간 성격 |
| `link.length` / `link.time` | 구간 속도 | ❌ 지형 난이도 추정 |
| `section.calories` | | ❌ 거리의 함수로 보임 |

**C. 로드뷰 반경 검색** (→ `21-roadview-providers.md` §1.3c)

| 라벨 | 값 | 지금 쓰나 |
|---|---|---|
| `shot_tool` | 촬영 장비·기관 | ⚠️ 사후 조회만. **캡처 전에 봐야 한다** |
| `shot_date` | 촬영 일시 | ❌ 계절·시간대·노후도 |
| `img_path` 의 세션 id | 같은 주행 묶기 | ❌ 표본 독립성 검사 |
| `angle` | 촬영 기준 방위 | (화각 계산에 간접 사용) |
| `addr` | 지번 주소 | ❌ |
| `st_name` `st_type` `area_type` | 도로명·등급·지하도 | ❌ **§1.3c — 판정에 쓰지 마라** |
| 반경 안 pano 밀도 | 도보/차량 개수 | ❌ 커버리지 지표 |
| 최근접 도보 pano 까지 거리 | | ❌ **대체 가능성** |

**D. 로드뷰 단건**

| 라벨 | 값 | 지금 쓰나 |
|---|---|---|
| `spot[]` | 이웃 pano (`shot_tool` 은 **없다**) | ❌ 그래프 탐색 |
| `past[]` | 같은 지점 과거 촬영본 + 각각의 `shot_tool` | ❌ **계절 강건성 시험** |

**E. Kakao Local REST**

| 라벨 | 값 | 지금 쓰나 |
|---|---|---|
| `coord2regioncode` | 법정동 `B` / 행정동 `H` 코드 | ❌ 지역 층화 |
| `coord2address.mountain_yn` | 산 지번 | ❌ 약한 신호 (아래) |
| `category` 반경 검색 | `AT4` 관광명소 · `PK6` 주차장 · `FD6` 음식점 … | ❌ 시가지/자연 대비 |

**F. 계산**

| 라벨 | 지금 쓰나 |
|---|---|
| `dist_to_route_m` (스냅 거리) | ✅ |
| `course_bearing` · `arrow_diff_deg` · `n_arrows` | ✅ |
| 코스 시작점부터의 진행 거리 | ❌ **head_m 편향을 정량화할 유일한 축** |
| 같은 코스 내 연속 표본 간 거리 | ❌ 표본 독립성 |

### 10.3 판별력 실측

가진 데이터로 실제로 재 본 것만 적는다. **날짜와 n 을 붙인다 — 조건이 바뀌면
이 숫자는 기록이지 기대값이 아니다.**

| 신호 | 도보 비율 | n | 판정 |
|---|---|---|---|
| `shot_tool ∉ {102,200,202}` | 정의상 100% | 914 | **정본.** 카카오 자신의 `isCar` |
| `existCenterLine = false` | 33% (vs `true` 3%) | 814 | **최고의 사전 게이트.** 공짜 |
| `st_type = None` | 88% (재현율 82%) | 914 | 보조 이하. 도보 33건을 버린다 |
| `st_type` + `st_name` 둘 다 None | 97% | 118 | 정밀하지만 재현율이 낮다 |
| `mountain_yn = Y` | 38% (vs `N` 10%) | 80 | **n 이 작다.** 8건뿐 — 참고만 |
| 반경 100m 안에 도보 pano | 30% | 60 | 대체 가능성 상한 |
| `past[]` 에 도보 계열 | 10% | 50 | 시간축 복구는 거의 안 된다 |

### 10.4 그래서 무엇을 바꾸나

**기록 (판단에 안 써도 남긴다).** `samples.jsonl` 에 C·D·F 를 전부 넣는다.
용량은 표본당 수백 바이트고, 없으면 재수집이다.

**게이트 (실제로 표본을 거른다).** 두 개만 쓴다. 순서가 중요하다:

1. **라우팅 직후** — `existCenterLine = true` 인 링크를 폴리라인에서 잘라낸다.
   API 호출 0회. 라우터가 차도로 우회한 구간이 여기서 사라진다
2. **스냅 시** — 반경 검색으로 후보를 받아 `snap_radius_m` 안의 **도보 pano 를
   우선**하고, 차량밖에 없으면 **그 점을 버린다**. 60m 떨어진 도보 pano 를
   끌어오지 않는다 — 그건 다른 길이다 (§8 "좌표를 지어내지 않는다")

**쓰지 않는다.** `st_type` · `st_name` · `mountain_yn` — 기록은 하되 게이트로
쓰지 않는다. 근거는 §10.3 의 n 과 `21-roadview-providers.md` §1.3c 의 반례다.

### 10.5 TODO

- [ ] `existCenterLine` 컷을 `fetch_walk_routes.py` 에 넣는다
- [ ] `make_samples.py` 의 `nearest()` 를 반경 검색 기반 도보 우선 스냅으로 교체
- [ ] 위 둘을 넣고 **재수집**. 표본이 914 → 200~300 으로 준다 (오염 80% 제거)
- [ ] `past[]` 로 계절 강건성 세트를 만든다 — 좌표가 같아 라벨이 유지된다
- [ ] `mountain_yn` 을 n≥300 으로 다시 재 본다

## 11. 도보 촬영분 확인법과 그 한계 (2026-08-20)

§10 에서 `shot_tool` 이 정본이라고 했다. **정본이 맞는지 사람 눈으로 확인한
기록**과, 확인해 보니 드러난 한계를 여기 적는다.

### 11.1 확인 절차 — 재현 가능하다

```
python app/labels/pano_meta.py --dataset seoul        # samples.jsonl → pano_meta.json
python app/labels/pano_meta.py --dataset jongno
python app/labels/pano_meta.py --walk-only seoul jongno   # → app/labels/walk_only/
```

- **재캡처가 없다.** pano id 만으로 `v2/node/{id}` 를 치므로 914장이 ~3분이다
- 도보 판정은 `CAR_TOOLS = {102, 200, 202}` 의 **여집합**이다 — 카카오
  `panorama.js` 의 `isWalk` 와 같다 (→ `21-roadview-providers.md` §1.3b).
  화이트리스트로 바꾸지 마라. 새 코드가 생기면 조용히 틀린다
- `walk_only/` 는 **하드링크**다. 용량이 늘지 않고, 지워도 원본이 남는다.
  gitignore 되어 있고 위 명령으로 언제든 다시 만든다

산출:

```
walk_only/
  index.jsonl            장별 course_id · pano_id · lat/lng · heading
                         · dist_to_route_m · shot_tool · st_type · shot_date
  seoul/tool100  99   tool205  61   tool101  21
  jongno/tool100 26   tool101  19
```

### 11.2 육안 확인 결과 (스팟체크, 226장 중 일부)

**`shot_tool` 은 실제로 촬영 방식을 맞힌다.** 도보로 분류된 장면들은 사람이
걸어서 찍은 것으로 보였다. §1.3b 의 코드표를 우리 데이터로 확인한 셈이다.

> 전수 검수가 아니라 **스팟체크**다. 아래 한계는 그 과정에서 눈에 띈 것이고,
> 비율은 재지 않았다.

### 11.3 한계 — "걸어서 찍었다" 는 "산책로다" 가 아니다

이게 이 절의 요점이다. `shot_tool` 이 보장하는 것은 **촬영 방식** 하나뿐이다.

1. **도보 촬영분 안에도 차도 장면이 있다.** 횡단보도를 건너는 지점, 도로변
   보도 구간이 그렇다. 배낭형 장비는 코스를 걷다가 차도를 건너기도 한다 —
   그 pano 는 도보 촬영이지만 화면은 도로다. **판정의 최종 근거는 VLM 이
   아니라 사람 검수로 남는다.**
2. **`205` 는 장비가 아니라 제공 기관(서울특별시)이다.** 지금 61장이 전부
   보행로인 것은 서울시가 넣은 데이터가 대부분 둘레길이기 때문이지, 코드가
   도보를 뜻해서가 아니다. 서울시가 차량 촬영분을 넣으면 이 상관은 깨진다
   (→ `21-roadview-providers.md` §1.3b 의 경고).
3. **표본이 쏠린다.** 도보 226장은 **27코스**에만 있고 상위 9코스가 71% 다:

   | 장수 | 코스 |
   |---:|---|
   | 22 | jongno-08 청계천길 |
   | 21 | seoul-179 난지 갈대바람길 |
   | 19 | seoul-142 태릉 백세길 · jongno-09 경복궁둘레길 |
   | 18 | seoul-099 선유도길 |
   | 16 | seoul-183 서초구 양재천 |
   | 15 | seoul-133 어린이대공원 · seoul-127 소나무 숲길 · seoul-032 영등포 수변둘레길 |

   하천·공원·도심 산책로가 독식한다. 반대편은 인왕산 1 · 벚꽃십리길 1 ·
   봉산 능선 2 · 대모산 2 로 바닥이다. §9 에 적은 "이 세트는 하천·시가지 쪽으로
   기울어 있다" 가 **도보 필터를 걸면 더 심해진다.** 나머지 18코스는 코스당
   1~8장이라 코스 단위 정확도를 낼 수 없다.

### 11.4 차량 촬영분을 버리지 않는다

**차도 위 pano 에서도 산책로가 보이는 경우가 있다.** 청계천이 그렇다 —
위 도로에서 찍은 차량 pano 에 하천 산책로가 그대로 내려다보인다.
`shot_tool` 로 거르는 것은 **표본 오염을 줄이려는 것**이지, 그 장면들이
전부 음성이라는 뜻이 아니다.

그래서:

- 차량 촬영분 이미지를 **삭제하지 않는다.** `<ds>/images/` 에 그대로 둔다
- 수집 파이프라인의 `shot_tool` 게이트는 **새 표본을 고를 때**만 적용한다
  (→ §10.4). 이미 모은 것을 소급해서 지우지 않는다
- 예외는 **§1.3c 의 강변북로 표본** 같은 것 — 터널이라 아무것도 안 보인다.
  이런 건 게이트가 아니라 검수에서 걸러야 한다

한 줄로: **`shot_tool` 은 표본을 고르는 도구지, 라벨이 아니다.**
